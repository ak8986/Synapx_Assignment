# Synapx_Assignment
An autonomous insurance claims processing agent that extracts key fields from FNOL documents, detects missing information, classifies claims, and recommends the correct routing workflow using rule-based logic.
